#version 330 core

layout(location = 0) out vec4 color;

uniform vec4 color4;
uniform sampler2D textureSampler;

in vec2 textureCoordinatesToFragment;

void main()
{
	vec4 textureColor = texture(textureSampler, textureCoordinatesToFragment);
	color = textureColor * color4;
};